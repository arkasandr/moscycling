api:
  key: 10d4289dc1d35470807639f4a045528b
spring:
  jpa:
    generate-ddl: true
    show-sql: true
    hibernate:
      ddl-auto: create-drop
#      ddl-auto: validate
  datasource:
    hikari:
      connectionTimeout: 20000
      maximumPoolSize: 5
    platform: postgres
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:moscycling}
    username: ${DB_LOGIN:postgres}
    password: ${DB_PASSWORD:p091ulp}
    driver-class-name: org.postgresql.Driver
  liquibase:
    enabled: true #this is optional as enabled by default
    change-log: classpath:/db/db-changelog.xml
server:
  port: 9090



